# Count Inversions

## Problem

Given an array of `N` distinct integers, find the total number of inversions in the array.

An inversion is a pair of indices `(i, j)` such that:

- `i < j`
- `arr[i] > arr[j]`

## Example

### Input

```text
3
3 2 1
